apiVersion: apps/v1
kind: Deployment
metadata:
  name: tomcat-deployment
  labels:
    app: 
spec:
  replicas: 3
  selector:
    matchLabels: 
      app: tomcat
  template:
    metadata:
      labels:
        app: tomcat
    spec:
      containers:
      - name: tomcat
        image: tdiya0311/devops:tomcat
        ports:
        - containerPort: 8080